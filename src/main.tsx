import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../gaon_ki_saloon_landing_page';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
